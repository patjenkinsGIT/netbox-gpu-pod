# Pins the NetBox image tag for both netbox and netbox-worker.
# The worker inherits the netbox service via a YAML anchor, so setting it
# once here keeps app and worker in lockstep.
VERSION=v4.7-5.1.0

# Pin the compose project name so volume names never depend on directory name.
COMPOSE_PROJECT_NAME=netbox-docker
